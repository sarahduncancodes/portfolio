module.exports = {
    partialHelper: function (id) {
        if (id === "1") { return "roi"; }
        if (id === "2") { return "notifications"; }
        if (id === "3") { return "portfolio"; }

    }
}